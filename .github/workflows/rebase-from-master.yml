name: Rebase from Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} from master"

jobs:
  rebase:
    uses: ./.github/workflows/_rebase-from-master.yml
    permissions:
      contents: write
    secrets: inherit
